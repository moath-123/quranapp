package com.codesteem.quransdk

import androidx.appcompat.app.AppCompatActivity

/** Empty host so instrumented tests can attach [com.codesteem.quransdk.ui.QuranPageView] to a real window. */
class TestHostActivity : AppCompatActivity()
